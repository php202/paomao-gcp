# 你的步驟對照執行指南 — 檢查與注意

## 一、你已完成的項目 ✓

| 項目 | 你的狀態 | 說明 |
|------|----------|------|
| 專案 ID | gen-lang-client-0828139766 | ✓ |
| 試算表共用用的服務帳戶 | paomao-967@gen-lang-client-0828139766.iam.gserviceaccount.com | 見下方「重要」 |
| Secret Manager | saydou-token（待從表單取得值）、gmail-user ✓、gmail-app-password ✓ | saydou-token 建立後要授權 |
| Secret 授權給 Cloud Run | gmail-user、gmail-app-password 已可讀 | **saydou-token 也要**授權給「跑 Job 的服務帳戶」 |
| 本機 gcloud | 登入、專案、Artifact Registry、builds submit | ✓ |

---

## 二、重要：服務帳戶要一致

- **試算表**你共用給：`paomao-967@gen-lang-client-0828139766.iam.gserviceaccount.com`
- **Cloud Run Job 預設**是用「專案預設 Compute 服務帳戶」跑：  
  `專案編號-compute@developer.gserviceaccount.com`

若 Job 用預設 Compute 帳戶跑，但試算表只共用給 paomao-967，Job 會**讀不到試算表**。

**請二選一：**

1. **改試算表共用**：把「員工清單、店家、Token、產出」也共用給  
   `專案編號-compute@developer.gserviceaccount.com`（編輯者），  
   查專案編號：`gcloud projects describe gen-lang-client-0828139766 --format='value(projectNumber)'`
2. **改 Job 用 paomao-967 跑**：建立 Job 時加  
   `--service-account=paomao-967@gen-lang-client-0828139766.iam.gserviceaccount.com`  
   並且 **Secret Manager 的 saydou-token、gmail-user、gmail-app-password 都要授權給 paomao-967**（不是只授權給 default compute）。

執行指南裡是用「預設 Compute 帳戶」做 Secret 授權與 Scheduler，若你選 (2)，腳本也要改成用 paomao-967。

---

## 三、SayDou Token：改為每次從試算表讀取（不必用 Secret）

**pao-check-token** 與 **pao-employee-report** 已改為**不依賴 saydou-token Secret**，每次執行時從試算表讀取 Token（Token 常變動也沒問題）。

- 試算表 ID 設在 Job 的環境變數 **TOKEN_SHEET_SS_ID**。
- 程式會讀取該試算表、工作表 **「預約表單」**、儲存格 **C2** 的值當作 SayDou Bearer Token。
- 請把這張 Token 試算表「共用」給跑 Job 的服務帳戶（編輯者或檢視者即可）。

若你仍想用 Secret 存 Token，可改回 `--set-secrets "SAYDOU_BEARER_TOKEN=saydou-token:latest,..."` 並在 set-env 不設 TOKEN_SHEET_SS_ID；多數情況用試算表較方便。

---

## 四、接著照執行指南：建立 Job + Scheduler

執行指南「三、依序執行」裡的 **4. 建立兩個 Cloud Run Job** 和 **5. 給 Scheduler 權限**、**6. 建立 Cloud Scheduler** 可以：

- **手動**：在 `gcp` 目錄下，把 執行指南 的指令一段段複製到終端機執行（注意貼上時不要有前後空白導致整行被當成一個指令）。
- **或用腳本**：已幫你寫好 `setup-jobs-and-scheduler.sh`，會依執行指南建立 Job 與 Scheduler。  
  執行前請先：
  1. `source set-env.sh`
  2. 在 `set-env.sh` 或本機 export 設定好四個試算表 ID：  
     `LINE_STAFF_SS_ID`、`LINE_STORE_SS_ID`、`OUTPUT_SS_ID`、`TOKEN_SHEET_SS_ID`  
     （執行指南裡有範例 ID，請改成你的實際 ID）

執行指南裡 Job 2 的 `--set-env-vars` 有試算表 ID，務必改成你的實際值，否則月報會連錯試算表。

---

## 五、簡短檢查清單（依序打勾）

- [ ] 試算表共用：已共用給「真正會跑 Job 的那個服務帳戶」（預設 Compute 或 paomao-967，二選一且一致）
- [ ] Token 試算表：已共用給跑 Job 的服務帳戶，「預約表單」C2 放 SayDou Bearer Token（Job 每次從這裡讀，常變動也沒問題）
- [ ] gmail-user、gmail-app-password：已建立且已授權（你已完成）
- [ ] 建立兩個 Job：pao-check-token（從試算表讀 Token）、pao-employee-report（用試算表 ID）
- [ ] Scheduler 權限：兩個 Job 都加了 `run.invoker` 給 `${PROJECT_NUMBER}-compute@...`
- [ ] 建立兩個 Scheduler：pao-check-token-daily、pao-report-monthly
- [ ] 在 Console 手動執行一次 pao-check-token、一次 pao-employee-report 確認沒錯誤

若在某一步卡住（例如找不到服務帳戶、Secret 授權失敗、gcloud 錯誤），把「第幾步、畫面或錯誤訊息」貼出來，可以一步步幫你排查。
