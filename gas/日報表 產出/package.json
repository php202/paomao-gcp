{
  "name": "daily-report",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "push": "clasp push --force",
    "deploy": "echo '[日報表 產出] 無 Web 部署，僅 push'",
    "ship": "npm run push",
    "pull": "echo '請勿 pull；以本機為準，只做 npm run push 或 npm run ship。' && exit 1"
  }
}
